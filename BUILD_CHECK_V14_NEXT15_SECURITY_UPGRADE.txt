V14 NEXT 15 SECURITY UPGRADE BUILD CHECK
========================================

Source baseline:
- portfolio-reference-collage-v13-baseline-cleanup.zip

Requested build:
- portfolio-reference-collage-v14-next15-security-upgrade.zip

Changes made:
1. Upgraded Next.js from 14.2.5 to 15.5.18.
2. Kept React and React DOM on 18.3.1 to minimize framework-change surface area.
3. No UI, layout, route, content, component, or animation changes were made.
4. Kept the optimized motion block in app/globals.css as the motion source of truth.
5. Left placeholder case-study images unchanged for a later content/image pass.

Install/build commands run:
- npm install next@15.5.18
- npm run build
- npm audit --omit=dev

Build result:
- npm run build completed successfully.
- Next.js version shown during build: 15.5.18
- Routes generated:
  - / static
  - /projects static
  - /projects/[slug] dynamic/server-rendered on demand

Audit note:
- npm audit still reports 2 moderate PostCSS-related advisories through Next.js.
- npm audit suggests npm audit fix --force, but that would downgrade/install next@9.3.3 according to npm's output, so it was NOT applied.
- This build intentionally avoids forced/breaking audit fixes.

Packaging note:
- node_modules and .next are excluded from the ZIP.
- package-lock.json was regenerated by npm install.
