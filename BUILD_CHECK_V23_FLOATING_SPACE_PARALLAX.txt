Build: portfolio-reference-collage-v23-floating-space-parallax.zip
Base: portfolio-reference-collage-v22-clean-merge.zip
Date: 2026-06-13

Requested change:
- Main hero cards should move slightly all the time, like floating in space.
- Main hero cards should parallax in different directions on scroll.

Changes made:
- Added a controlled ambient float layer for the three homepage hero cards only.
- Added scroll parallax variables for the three homepage hero cards, each with a different x/y/rotation direction.
- Integrated the new motion into the existing card transform formulas using CSS variables:
  --float-x, --float-y, --float-r
  --scroll-x, --scroll-y, --scroll-r
- Kept drag/tap/front-card logic unchanged.
- Kept iOS Safari long-press link-preview fix unchanged.
- Kept project cards and service cards out of the new ambient loop to avoid reintroducing sideways scroll drift.
- Kept reduced-motion protection: ambient float and scroll parallax do not run when prefers-reduced-motion is enabled.

Build commands run:
- npm ci: completed successfully with the existing 2 moderate audit warnings.
- npm run build: compiled successfully and generated static pages (10/10), but the sandbox timed out during/after "Collecting build traces", matching the prior v22 clean-merge behavior.

Important note:
- This ZIP should be tested on Vercel and iPhone Safari before being treated as the confirmed baseline.
