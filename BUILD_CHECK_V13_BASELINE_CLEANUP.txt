Build check for portfolio-reference-collage-v13-baseline-cleanup

Changes applied:
1. Added missing React key={project.slug} to homepage project card links in app/page.js.
2. Removed duplicate data-project-stack attribute from project-card-stage in app/page.js.
3. Changed ABOUT navigation link from #about to /#about in components/Nav.js.
4. Preserved the Phase 2 optimized motion block in app/globals.css as the active motion source of truth.
5. Left placeholder case-study images unchanged intentionally; replace with local project assets in a later image pass.

Clean install/build commands run:
rm -rf node_modules .next
npm ci
npm run build

Result:
Build completed successfully.

Notes:
- npm ci completed successfully.
- npm reported that next@14.2.5 is deprecated and flagged 2 audit vulnerabilities. This was not changed in this cleanup build to avoid altering framework behavior beyond the requested baseline cleanup.
- The production build compiled successfully and generated all app routes.
